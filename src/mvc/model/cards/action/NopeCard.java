package mvc.model.cards.action;

import mvc.model.cards.ActionCard;

public class NopeCard extends ActionCard {

    @Override
    public void performAction() {
//        Game.PlayedCard playedCardToCancel = game.getPlayedCards()
//                .subList(0, game.getPlayedCards().size() - 1).getLast();
//        System.out.println("Cancelling " + playedCardToCancel.getCard() + " played by " + playedCardToCancel.getPlayedBy());
//        playedCardToCancel.cancel();
    }
}
